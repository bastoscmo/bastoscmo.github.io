# Authors
#
# Optional per-post/page author overrides. If a post or page sets an
# `author:` field matching a key below, its byline uses these details
# instead of the site-wide `author:` block in _config.yml. Unused while
# every page is by the single site author.
